<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hc.mapper.askRecord.TbAskRecordMapper" >
	  
	<select id="getAskRecord" resultType="com.hc.pojo.askRecord.TbAskRecord">
		SELECT tb_id as tbId,tb_user_id as tbUserId,tb_number as tbNumber,tb_name as tbName,create_time as createTime FROM tb_ask_record
		<if test="page != null and size != null">
			LIMIT #{start}, #{size}
		</if>
	</select>
	
	<select id="getAskRecordCount" resultType="java.lang.Integer">
		SELECT COUNT(1) FROM `tb_ask_record`
	</select>
	
	<insert id="insertSelective" keyProperty="id" useGeneratedKeys="true">
	INSERT INTO `tb_ask_record` ( `tb_user_id`, `tb_number`, `tb_name`, `tb_enclosure`, `tb_history`, `tb_draft`, `del_time`, `create_time`, `tb_desc` )
	VALUES
		( #{tbUserId}, #{tbNumber}, #{tbName}, NULL, NULL, NULL, NULL, now(), NULL );
	</insert>
	
</mapper>