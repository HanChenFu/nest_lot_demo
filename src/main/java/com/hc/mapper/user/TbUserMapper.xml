<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hc.mapper.user.TbUserMapper" >
	<sql id="Base_Column_List">
		user_id, user_uuid, user_login_id, user_pwd, user_pwd_md5,
		user_name, user_qq,
		user_wechat,
		user_phone, user_status, user_type,
		user_time,tenant
	</sql>
  
	<select id="checkUserLoginId" resultType="com.hc.pojo.base.TbAdmin">
		select
		user_id as tbId,user_uuid as userUuid,user_time as createTime
		from sys_user_info
		where user_login_id =
		#{tbName,jdbcType=VARCHAR} and user_pwd =
		#{tbPassword,jdbcType=VARCHAR}
	</select>
  
</mapper>